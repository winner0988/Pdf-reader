# CONTEXT.md

專案詞彙表：只收錄這個專案裡被賦予特定意義、容易產生歧義的詞。

## 表單腳本沙盒（Form Script Sandbox）
本專案中，「表單腳本沙盒」定義為：僅能讀寫當前文件表單欄位的數值並觸發重新計算的執行環境；不得發出網路請求、存取檔案系統、啟動外部程式。此沙盒只在使用者主動與表單互動（如輸入、切換欄位）時才啟動，不回應 /OpenAction 或 /AA 之類開檔即自動觸發的動作。

## 信任（單一文件）(Trusting a Document)
本專案中，「信任」一份文件是指使用者手動允許該文件內引用的遠端資源被載入（見 ADR 0002）。信任的判斷依據是文件內容的雜湊值：狀態會跨次啟動保留，但只要文件內容改變（雜湊值不同），先前的信任就會失效，需要重新確認。

## 文件識別碼 (Document ID)
本專案中，任何需要「跨次啟動、搬移改名都不失效」地識別同一份 PDF 的功能，都透過寫入該 PDF 中繼資料中的一個應用程式產生的識別碼來達成，而非依賴檔案路徑或內容雜湊。這與「信任（單一文件）」刻意不同——信任必須在內容改變時失效（見 ADR 0002），因此仍以內容雜湊為準；文件識別碼則是文件本身持久的屬性，編輯內容不影響它。機敏標記（見下）與加密檔案的密碼記憶（見 ADR 0006）都依附在文件識別碼上。

## 機敏文件 (Sensitive File)
使用者手動標記為需要加強保護的 PDF（見 ADR 0004）。標記依附在該文件的文件識別碼上，因此搬移或改名檔案都不會遺失標記。只有機敏文件會啟用系統層級外洩防護（縮圖快取關閉、剪貼簿保護）。「隱私匯出」產生的副本不會繼承這個標記，因為隱私匯出的目的正是清除所有中繼資料（含文件識別碼本身）；原始工作檔案上的標記不受影響。

## 批次處理背景服務 (Batch Processing Background Service)
本專案中，「背景服務」僅指：使用者主動觸發批次處理佇列後，允許關閉主視窗、讓佇列在背景（含系統列圖示）繼續執行至完成（見 ADR 0005）。佇列清空即自動結束，不隨作業系統開機自動啟動，也不會在沒有進行中的批次工作時常駐背景。與「無遙測」原則不衝突——它不做任何監控、回報或持續運行的行為。
