{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the main window. Intentionally empty: the frontend gets no system APIs until a work card needs one (AGENTS.md principle 6). Any addition needs the needs-security-review label.",
  "windows": ["main"],
  "permissions": []
}
