{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the main window: only the app's own document commands (MVP-06). No core:event, dialog or fs permissions: paths must never reach the WebView. Any addition needs the needs-security-review label.",
  "windows": ["main"],
  "permissions": [
    "allow-subscribe-open-events",
    "allow-open-document-dialog",
    "allow-retry-open",
    "allow-close-document"
  ]
}
