[package]
name = "worker_host"
description = "Main-process side of the pdf_worker protocol: sandboxed start, handshake, validation, timeouts, restart (ADR 0008)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
ipc_contract = { path = "../ipc_contract" }
sandbox = { path = "../sandbox" }
thiserror = "2"
