# Cargo and npm entries are added by MVP-01 once manifests exist
# (Dependabot reports errors for ecosystems without a manifest).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
    labels:
      - dependencies
      - needs-security-review
